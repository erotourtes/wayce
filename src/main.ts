import "./config.js";

import search from "./Engine/Engine.js";

const res = await search("words is going to nvim");
// const res1 = await search("read some words");

// console.log(res);
// console.log(res1);
